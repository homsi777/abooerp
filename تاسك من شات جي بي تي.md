اسم التاسك:
تحويل نظام شركة الشحن من Web Browser إلى تطبيق Windows Desktop حقيقي يعمل Offline‑First مع مزامنة آمنة وموثوقة مع الخادم المركزي

المشروع:
C:\Users\Homsi\Desktop\almiya-hsahin

الدور المطلوب منك:
أنت المسؤول التقني الكامل عن تحليل وتنفيذ واختبار التحويل. نفّذ العمل باحترافية عالية، ولا تعتمد على افتراضات غير مثبتة. افحص الكود والمخطط الحي والترحيلات ومسارات الكتابة قبل تعديل أي جزء.

==================================================
1. الهدف الرئيسي
==================================================

تحويل نظام شركة الشحن الحالي من نظام يعتمد على المتصفح والاتصال المستمر بالإنترنت إلى تطبيق سطح مكتب Windows يعمل بمنهج Offline‑First.

المشكلة التشغيلية الحالية:

- اتصال الإنترنت في فروع الشركة ينقطع بصورة متكررة.
- موظف إدخال البيانات يكتب أسطر وإيصالات الشحن داخل دفتر الشحن اليومي.
- عند انقطاع الشبكة قد يفشل إرسال الطلب أو تبقى العملية غير مؤكدة.
- الموظف لا يعرف بصورة موثوقة هل وصلت البيانات إلى الخادم أم لا.
- إعادة المحاولة قد تؤدي إلى التكرار، بينما عدم إعادة المحاولة قد يؤدي إلى فقد البيانات.
- IndexedDB الموجودة حاليًا تغطي مسودات محدودة من دفتر الشحن، وليست محرك مزامنة كاملًا ودائمًا.
- المطلوب ألا تعتمد سلامة بيانات الموظف على استمرار الإنترنت.

النتيجة المطلوبة:

1. يحفظ التطبيق كل إدخال أولًا داخل قاعدة بيانات محلية دائمة.
2. يعتبر الحفظ المحلي الناجح هو نجاح عملية الإدخال بالنسبة للموظف.
3. يستمر الموظف في العمل عند انقطاع الإنترنت.
4. تُحفظ العمليات غير المرسلة في Outbox دائم داخل نفس معاملة الحفظ المحلي.
5. عند عودة الإنترنت، يرفع التطبيق العمليات تلقائيًا إلى الخادم المركزي.
6. يمنع الخادم إنشاء البيانات مرتين مهما تكررت المحاولة.
7. يسحب التطبيق التغييرات المعتمدة من المركزي بصورة تدريجية.
8. تظهر للمستخدم حالة المزامنة الحقيقية وعدد العمليات والنسبة المئوية.
9. لا تظهر نسبة 100% إلا بعد تأكيد الخادم المركزي استلام جميع العمليات.
10. لا تُحذف البيانات المحلية غير المزامنة تحت أي ظرف تلقائي.

==================================================
2. تعليمات إلزامية لا يجوز مخالفتها
==================================================

- ممنوع تخريب تصميم الواجهة الأمامية أو إعادة تصميم الصفحات.
- ممنوع تغيير بنية الصفحات أو أسماء الحقول أو النصوص العربية إلا عند الحاجة المباشرة لإظهار حالة الحفظ والمزامنة.
- حافظ على RTL والتصميم والألوان والمكونات الحالية.
- أضف واجهة المزامنة بأقل تدخل بصري ممكن.
- لا تحوّل التطبيق إلى مجرد WebView يحتاج إلى الإنترنت.
- لا تعتبر Electron وحده حل Offline‑First.
- يجب وجود قاعدة محلية دائمة ومحرك مزامنة حقيقي.
- ممنوع استخدام SQLite. المطلوب PostgreSQL محلي لتقليل اختلاف الأنواع والقيود عن PostgreSQL المركزي.
- لا تجعل واجهة Renderer تتصل مباشرة بقاعدة PostgreSQL.
- جميع اتصالات قاعدة البيانات تمر من Backend محلي موثوق.
- لا تفتح PostgreSQL المحلي على الشبكة؛ يجب أن يستمع على loopback فقط.
- لا تضع كلمة مرور قاعدة البيانات داخل كود الواجهة أو المستودع.
- لا تستخدم توقيت جهاز Windows وحده لترتيب العمليات.
- ممنوع اعتبار CONFLICT حالة تشغيلية للشحنة داخل `shipments.status`.
- لا تنفذ المالية أو المخزون أو الحوالات المالية محليًا.
- لا تغير المعادلات المحاسبية أو سلوك الترحيل المالي القائم.
- لا تطبق أي migration على قاعدة إنتاج حية دون تحقق ونسخة احتياطية وإذن واضح.
- افحص `git status` قبل العمل وحافظ على تعديلات المستخدم الموجودة.
- لا تنهِ المهمة بمجرد نجاح build؛ يجب اختبار العمل Offline وOnline فعليًا.

==================================================
3. القرار المعماري الأساسي
==================================================

استخدم المعمارية التالية ما لم يثبت فحص المستودع وجود بنية Desktop قائمة تؤدي الغرض نفسه:

1. تطبيق Windows Desktop باستخدام Electron والبنية الحالية للمشروع.
2. الإبقاء على React والواجهة الحالية دون إعادة تصميم.
3. تشغيل Backend محلي داخل التطبيق أو كعملية محلية يديرها تطبيق سطح المكتب.
4. تشغيل PostgreSQL محلي كخدمة Windows أو Runtime محلي مُدار بطريقة آمنة.
5. تتصل الواجهة بالـLocal Backend فقط في مسارات Offline‑First.
6. يتولى Local Backend:
   - الحفظ المحلي.
   - التحقق من البيانات.
   - إدارة المعاملات.
   - كتابة Outbox.
   - قراءة الحالة المحلية.
   - تشغيل عامل المزامنة.
7. يتصل عامل المزامنة بالخادم المركزي عبر HTTPS API فقط.
8. استخدم API-based bidirectional incremental sync.
9. لا تستخدم PostgreSQL logical replication بين أجهزة الموظفين والمركزي.
10. لا يحتاج جهاز الموظف إلى اتصال PostgreSQL مباشر بالخادم المركزي.

قبل التنفيذ، افحص إن كان المشروع يحتوي بالفعل على Electron أو Desktop runtime أو Local server. أعد استخدام الموجود إن كان سليمًا بدل إنشاء بنية موازية غير ضرورية.

==================================================
4. نطاق Offline‑First
==================================================

الأولوية الأولى والحاسمة هي بيانات موظف الإدخال في دفتر الشحن اليومي، وخاصة أسطر الشحنات.

صنّف الجداول والكيانات إلى ثلاث مجموعات واضحة ودوّن التصنيف في تقرير التنفيذ:

A. Local Writable:

- `daily_ledger_sessions`
- `daily_ledger_rows`
- `daily_ledger_dispatch_definitions`
- `daily_ledger_row_transfers`
- `daily_ledger_row_transfer_items`
- سجلات الطباعة المحلية اللازمة لاستمرار العمل:
  - `daily_ledger_print_events`
  - `daily_ledger_print_documents`
- بيانات Outbox وحالة المزامنة والتعارضات.

أي كتابة Offline في هذه المجموعة يجب أن تكون محلية أولًا ثم تُزامن.

B. Local Read‑Only Cache:

نزّل فقط البيانات المرجعية اللازمة لإدخال السطر والتحقق منه، وفق نطاق المستخدم:

- الشركة الحالية.
- الفروع المسموح بها.
- الوكلاء المسموح بهم.
- المرسلون والمستلمون اللازمون للعمل.
- المدن والوجهات.
- العملات.
- أنواع البضائع.
- السائقون والمركبات عند الحاجة.
- الإعدادات اللازمة للدفتر والطباعة.
- الحد الأدنى من بيانات المستخدم والدور والصلاحيات اللازمة للعمل Offline.

C. Central‑Only:

تبقى العمليات التالية مركزية ولا تُنفذ محليًا أثناء انقطاع الإنترنت:

- جدول `transfers` الخاص بالحوالات المالية.
- الترحيل المالي.
- `party_financial_movements`
- `cashbox_transactions`
- سندات القبض والدفع.
- حجز أو خصم المخزون.
- الآثار المالية والمخزنية التابعة للشحنة.
- التسويات والتحصيلات.
- أي عملية حساسة لا يمكن ضمان قواعدها دون الحالة المركزية.
- العمليات الإدارية الحساسة وتغيير الصلاحيات.
- أي إجراء طباعة يكتب أثرًا ماليًا أو مركزيًا غير متاح محليًا.

مهم جدًا:

`transfers` تعني الحوالات المالية وتبقى مركزية.

أما `daily_ledger_row_transfers` فهي نقل تشغيلي لأسطر الدفتر بين الجلسات، وليست حوالة مالية، ويمكن إدراجها ضمن المزامنة المحلية بعد تطبيق قواعدها كاملة.

إذا أثبت الفحص أن أحد الجداول المذكورة يحتاج تصنيفًا مختلفًا، لا تغيّر التصنيف بصمت. وثّق الدليل والتأثير واختر الحل الأكثر أمانًا لبيانات موظف الإدخال.

==================================================
5. نطاق البيانات المحلية والصلاحيات
==================================================

لا تنزّل نسخة كاملة من بيانات الشركة إلى كل جهاز.

استخدم Scoped Local Dataset وفق التالي:

- بيانات فرع الجهاز الأساسي.
- الفروع المسموح بها للمستخدم حسب `DataScope`.
- بيانات الوكيل فقط إذا كان المستخدم مرتبطًا بوكيل.
- البيانات المرجعية اللازمة للعمل ضمن هذا النطاق.
- عدم تنزيل بيانات فروع لا يملك المستخدم صلاحية الوصول إليها.

أنشئ سجل جهاز مركزي يحتوي على الأقل:

- `device_id` من نوع UUID.
- اسم الجهاز.
- الفرع الأساسي.
- المستخدم الذي سجل الجهاز.
- تاريخ التسجيل.
- آخر اتصال.
- آخر مزامنة ناجحة.
- حالة الجهاز: active / disabled / revoked.
- إصدار التطبيق.
- إصدار المخطط المحلي.
- آخر Central Cursor.
- بيانات تعريف آمنة دون حفظ أسرار مكشوفة.

المصادقة Offline:

- أضف مدة سماح قابلة للضبط للعمل Offline بعد آخر مصادقة مركزية ناجحة.
- استخدم إعدادًا واضحًا مثل `OFFLINE_AUTH_MAX_AGE_HOURS`.
- لا تخزن كلمة مرور المستخدم بصيغة قابلة للاسترجاع.
- خزّن Offline authorization grant مشفرًا ومحدود الصلاحية.
- عند انتهاء مدة السماح، امنع الكتابة الجديدة الحساسة حتى تتم مصادقة مركزية.
- لا تحذف البيانات غير المزامنة بسبب انتهاء المصادقة.
- عند تعطيل الجهاز مركزيًا، يجب رفض مزامنته عند عودة الاتصال.
- عند سحب صلاحية المستخدم، لا ترسل له بيانات جديدة خارج نطاقه.
- عالج البيانات المحلية القديمة بطريقة آمنة دون فقد Outbox غير المرفوع.

==================================================
6. الهوية وتوليد الأرقام
==================================================

استخدم UUID الموجود في المفاتيح الأساسية كهوية عالمية للسجلات.

لا تعتمد على:

- `Date.now()`
- timestamp وحده.
- `max()+1` كهوية عالمية.
- رقم قابل للتصادم بين جهازين.

لكل عملية مزامنة استخدم:

- `operation_id`: UUID دائم.
- `device_id`: UUID الجهاز.
- `device_sequence`: رقم متزايد لا يتكرر داخل الجهاز.
- `entity_type`
- `entity_id`
- `operation_type`
- `payload`
- `payload_hash`
- `committed_at`
- `sync_status`
- عدد المحاولات.
- آخر خطأ.
- وقت المحاولة التالية.

ترتيب الإرسال يعتمد على `device_sequence` وليس ساعة Windows.

بالنسبة إلى:

- `daily_ledger_rows.row_no`
- `daily_ledger_dispatch_definitions.dispatch_no`

أبقِهما أرقامًا ترتيبية محلية للعرض والترتيب داخل سياقهما.

لا تحولهما إلى نصوص.

إذا احتاجت المزامنة رقمًا نصيًا قابلًا للعرض، أضف حقولًا مستقلة مثل:

- `row_sync_no`
- `dispatch_sync_no`

مع صيغة يمكن أن تعتمد على:

`{branch_code}-{device_short_id}-{local_sequence}`

لكن لا تستخدم هذه الحقول بدل UUID الأساسي.

عالج سباقات `max()+1` باستخدام sequence محلي أو قفل مناسب أو آلية تخصيص مؤكدة، ولا تكتفِ بانتظار خطأ UNIQUE.

==================================================
7. Local Outbox
==================================================

أنشئ Outbox دائمًا داخل PostgreSQL المحلي.

الشرط الأساسي:

حفظ السطر وكتابة عملية Outbox يجب أن يحدثا داخل نفس Database Transaction.

إما ينجح الاثنان أو يفشلان معًا.

الحالات المقترحة:

- `PENDING`
- `SENDING`
- `ACKNOWLEDGED`
- `RETRY`
- `CONFLICT`
- `REJECTED`
- `BLOCKED`

يجب أن يحتوي السجل على:

- operation ID.
- device ID.
- device sequence.
- entity type.
- entity ID.
- action.
- payload.
- payload version.
- payload hash.
- local base version.
- committed timestamp للتوثيق.
- attempt count.
- next retry time.
- last error code.
- last error message الآمن.
- acknowledged central version.
- created/updated timestamps.

القواعد:

- لا تحذف Outbox ACKNOWLEDGED مباشرة.
- استخدم retention واضحًا للتدقيق.
- لا تفقد PENDING/RETRY/CONFLICT عند إغلاق التطبيق أو إعادة تشغيل Windows.
- عالج العمليات العالقة في SENDING بعد انهيار التطبيق.
- استخدم exponential backoff مع jitter.
- لا تنفذ حلقة retry عدوانية.
- راعِ ترتيب العمليات التابعة لبعضها.
- لا ترسل تعديل سجل قبل تأكيد إنشاء السجل نفسه.
- وفر زر إعادة محاولة آمن من واجهة حالة المزامنة.

==================================================
8. بروتوكول Push المركزي
==================================================

أنشئ Central Sync API بعقد واضح ومحدد الإصدار.

يجب أن يستقبل Batch من العمليات، لكن يعيد نتيجة مستقلة لكل عملية.

لكل عملية، أرسل:

- `operation_id`
- `device_id`
- `device_sequence`
- user/session authorization
- entity type/id
- operation
- payload
- request hash
- base version
- client schema version
- app version

أنشئ Durable Operation Result مركزيًا.

لا تكتفِ بمنطق `idempotency_keys` الحالي الذي يعيد 409 فقط.

عند إعادة إرسال `operation_id` نفسه:

- لا تنفذ العملية مرة ثانية.
- أعد النتيجة المركزية السابقة نفسها.
- أعد central entity ID/version.
- أعد الحالة النهائية للعملية.
- أعد بيانات التعارض إن وجدت.
- ارفض استخدام operation ID نفسه مع payload hash مختلف.

نتائج العملية على الأقل:

- `ACCEPTED`
- `ALREADY_APPLIED`
- `VALIDATION_REJECTED`
- `PERMISSION_REJECTED`
- `DEVICE_REVOKED`
- `DEPENDENCY_PENDING`
- `CONFLICT`
- `CENTRAL_PROCESSING_FAILED`

مهم:

إذا قُبل سجل الشحن أو الدفتر مركزيًا ثم فشل أثر مالي لاحق، فلا تعِد إنشاء سجل الشحن ولا تعتبر Business Sync كله فاشلًا.

==================================================
9. Central Change Feed وPull
==================================================

الـOutbox يحل اتجاه الجهاز إلى المركزي فقط، لذلك أنشئ آلية Pull تدريجية موثوقة.

أنشئ Central Change Feed أو Sync Log يتضمن:

- Central Cursor متزايدًا ورتيبًا.
- company ID.
- branch/agent scope.
- entity type.
- entity ID.
- operation type.
- authoritative version.
- authoritative payload أو مرجع يمكن جلبه.
- source device ID.
- created timestamp.
- tombstone للحذف.
- permission/scope metadata اللازمة للتصفية.

أنشئ Pull API يقبل:

- device ID.
- last cursor.
- batch size.
- permitted scope.
- schema/app version.

ويعيد:

- ordered changes.
- next cursor.
- has more.
- server timestamp.
- resnapshot required flag.

القواعد:

- لا تحدث Cursor المحلي إلا بعد تطبيق Batch كامل محليًا داخل معاملة.
- إذا فشل تطبيق Batch، لا تتقدم بالـCursor.
- طبّق Tombstones للحذف.
- امنع Echo الخاطئ مع الحفاظ على الحالة المركزية authoritative.
- ضع retention policy.
- إذا عاد جهاز بعد غياب أطول من retention، اطلب Scoped Resnapshot.
- يجب ألا تؤدي إعادة Snapshot إلى حذف Outbox غير المرفوع.
- ادمج Snapshot مع العمليات المحلية غير المرسلة بحذر.

==================================================
10. إدارة التعارضات
==================================================

لا تضف `CONFLICT` إلى `shipments.status`.

حالة الشحنة التشغيلية تبقى منفصلة عن حالة المزامنة.

أنشئ جدولًا مستقلًا للتعارضات يحتوي مثلًا على:

- conflict ID.
- operation ID.
- device ID.
- entity type/id.
- local payload/version.
- central payload/version.
- local intended action.
- central current state.
- conflict reason/code.
- status: OPEN / RESOLVED / REJECTED.
- resolution type.
- resolved by.
- resolved at.
- resolution notes.

خيارات الحل الإداري:

- اعتماد النسخة المركزية.
- رفض العملية المحلية.
- إعادة تطبيق الحدث المحلي إذا أصبح قانونيًا من الحالة المركزية الحالية.
- إنشاء حدث تصحيحي جديد.
- دمج يدوي للحقول التي يسمح بدمجها.

لا تستخدم Last‑Write‑Wins بصورة عمياء في:

- حالات الشحن.
- هوية الشحنة.
- المبالغ.
- الترحيل.
- نقل أسطر الدفتر.
- الحذف.
- العمليات المالية أو المخزنية.

يجب تحديد صلاحية خاصة لمن يستطيع حل التعارضات يدويًا، مع Audit Log كامل.

==================================================
11. حالات الأعمال والآثار المؤجلة
==================================================

افصل بين:

- نجاح مزامنة بيانات العمل.
- نجاح أو انتظار المالية.
- نجاح أو انتظار المخزون.
- نجاح أو انتظار العمليات المركزية الأخرى.
- وجود تعارض.

استخدم حالات واضحة، مثل:

- `BUSINESS_PENDING`
- `BUSINESS_SYNCED`
- `FINANCIAL_PENDING`
- `FINANCIAL_CONFIRMED`
- `FINANCIAL_FAILED`
- `INVENTORY_PENDING`
- `INVENTORY_CONFIRMED`
- `INVENTORY_FAILED`
- `CENTRAL_ACTION_PENDING`
- `CONFLICT`

لا تضع هذه الحالات داخل `shipments.status` إذا كانت حالات مزامنة أو معالجة.

الواجهة العربية تستخدم عبارات دقيقة، مثل:

- محفوظ محليًا.
- بانتظار الاتصال.
- بانتظار المزامنة.
- تمت مزامنة البيانات.
- بانتظار المزامنة المالية.
- تعذر إرسال بعض البيانات.
- يوجد تعارض يحتاج إلى مراجعة.

لا تستخدم كلمة «تم» قبل تأكيد الخادم المركزي.

==================================================
12. واجهة حالة المزامنة
==================================================

أضف بطاقة مزامنة صغيرة ومنبثقة أو قابلة للتوسيع دون إعادة تصميم الواجهة.

يجب ألا تكون Modal تعطل إدخال الموظف.

مكانها ينسجم مع التصميم الحالي، ويمكن أن تكون:

- مؤشرًا صغيرًا ثابتًا.
- بطاقة تظهر عند وجود مزامنة أو مشكلة.
- قابلة للطي والتوسيع لرؤية التفاصيل.

اعرض فيها:

- حالة الاتصال: متصل / غير متصل / جارٍ الفحص.
- حالة الحفظ المحلي.
- عدد العمليات الكلي المطلوب مزامنتها.
- عدد العمليات التي أكدها الخادم.
- عدد العمليات المتبقية.
- عدد العمليات الفاشلة.
- عدد التعارضات.
- نسبة مئوية حقيقية.
- آخر مزامنة ناجحة.
- وقت آخر محاولة.
- زر إعادة المحاولة.
- زر عرض التفاصيل عند وجود خطأ أو تعارض.

أمثلة النصوص:

عند انقطاع الإنترنت:
`تم حفظ البيانات محليًا — بانتظار عودة الاتصال`

أثناء المزامنة:
`جارٍ مزامنة البيانات مع السحابة — 120 من 150`

عند النجاح:
`تمت مزامنة جميع البيانات بنجاح`

عند الفشل الجزئي:
`تمت مزامنة 145 عملية، وتحتاج 5 عمليات إلى إعادة المحاولة`

حساب النسبة:

`acknowledged operations / total eligible operations * 100`

لا تحسب العملية ACKNOWLEDGED لمجرد إرسال HTTP request.

يجب أن يؤكد الخادم تطبيق العملية أو يعيد نتيجتها السابقة المؤكدة.

لا تظهر 100% إذا بقيت عمليات:

- PENDING
- SENDING
- RETRY
- CONFLICT
- BLOCKED

يمكن عرض مرحلتين عند الحاجة:

1. مزامنة بيانات الشحن.
2. المعالجة المالية/المركزية اللاحقة.

استخدم عبارة «مزامنة البيانات مع السحابة» بدل «رفع الملفات»، إلا إذا كانت العملية تتضمن ملفات فعلية.

==================================================
13. تجربة إدخال البيانات
==================================================

عند ضغط الموظف على إضافة أو حفظ سطر:

1. تحقق من البيانات محليًا.
2. افتح Transaction محلية.
3. احفظ أو حدث السطر محليًا.
4. أنشئ Outbox operation في المعاملة نفسها.
5. نفذ Commit.
6. أخبر الواجهة أن البيانات محفوظة محليًا.
7. نظف الحقول أو ركز على الحقل التالي وفق السلوك الحالي.
8. لا تنتظر الإنترنت لإكمال تجربة الإدخال.
9. شغّل المزامنة في الخلفية إن كان الاتصال متاحًا.
10. حدّث حالة السطر عند وصول تأكيد الخادم.

احمِ أزرار الحفظ من الضغط المتكرر.

استخدم Client Operation ID ثابتًا للعملية نفسها عند إعادة المحاولة.

لا تنشئ Operation ID جديدًا لمجرد أن الرد ضاع.

يجب أن يستعيد التطبيق جميع الأسطر والعمليات عند:

- إغلاق التطبيق.
- انهيار التطبيق.
- إعادة تشغيل Windows.
- انقطاع الكهرباء بعد Commit المحلي.
- انقطاع الشبكة أثناء إرسال الطلب.
- وصول الرد إلى الخادم وضياعه قبل وصوله إلى الجهاز.

==================================================
14. PostgreSQL المحلي والتثبيت
==================================================

أنشئ آلية موثوقة لإدارة PostgreSQL المحلي على Windows:

- تثبيت أو تجهيز PostgreSQL بالطريقة المناسبة للمشروع.
- تشغيله كخدمة Windows أو عملية مُدارة موثوقة.
- الاستماع على `127.0.0.1` فقط.
- مستخدم محدود الصلاحيات.
- كلمة مرور فريدة لكل جهاز.
- حفظ الأسرار باستخدام Windows secure storage أو آلية آمنة.
- عدم تسجيل كلمات المرور في logs.
- اكتشاف تعارض المنفذ ومعالجته.
- Health check محلي.
- بدء Backend بعد جاهزية PostgreSQL.
- إظهار رسالة عربية مفهومة إذا فشل تشغيل القاعدة.
- عدم فتح التطبيق على مخطط محلي غير متوافق.

نفّذ Local Migration Manager:

- يسجل إصدار المخطط المحلي.
- يشغّل migrations بالترتيب.
- يأخذ نسخة احتياطية قبل migration خطرة.
- يمنع تشغيل إصدار تطبيق جديد على schema أقدم إذا فشل الترحيل.
- يحافظ على Outbox والبيانات غير المزامنة.
- يدعم rollback أو recovery وفق نوع الخطأ.

التثبيت/الإزالة:

- لا تحذف البيانات المحلية غير المزامنة عند تحديث التطبيق.
- عند uninstall، حذر المستخدم إذا توجد عمليات غير متزامنة.
- لا تعِد استخدام Device ID قديم عشوائيًا بعد reinstall.
- وفر مسار استعادة مدروسًا.
- أنشئ Windows installer مناسبًا لـWindows 10/11.
- لا ترفع ملف التثبيت خارجيًا دون طلب.
- في النهاية أعطِ مسار ملف التثبيت المحلي وحجمه وSHA-256.

==================================================
15. النسخ الاحتياطي والاستعادة
==================================================

وفر نسخًا احتياطيًا محليًا مشفرًا، مع إعطاء الأولوية إلى:

- Outbox غير المرفوع.
- بيانات الدفتر المحلية غير المؤكدة.
- Device metadata.
- آخر Cursor.
- جداول التعارض.

حدد:

- توقيت النسخ.
- retention.
- مكان التخزين.
- التشفير.
- كيفية التحقق من سلامة النسخة.
- كيفية الاستعادة.

سيناريو الاستعادة:

1. تثبيت/إصلاح PostgreSQL المحلي.
2. جلب Scoped Snapshot جديد من المركزي.
3. استعادة Outbox غير المرفوع من النسخة الاحتياطية.
4. إعادة بناء التبعيات.
5. استئناف Push.
6. منع تكرار العمليات باستخدام Operation IDs السابقة.

وضّح أن فقد القرص قبل مزامنة البيانات وقبل إنشاء نسخة احتياطية يعني وجود Data‑Loss Window محلي لا يستطيع المركزي استرجاعه.

==================================================
16. معالجة نقاط عدم الاتساق الحالية
==================================================

افحص وعالج أو وثّق هذه النقاط قبل اعتماد المزامنة:

1. Migration رقم 111 موجودة في الكود وغير مطبقة في قاعدة البيانات المفحوصة، بينما توجد Routes تستخدم جدولها.
2. تعدد مصادر توليد `shipment_no`.
3. Global uniqueness للأرقام.
4. الآثار غير الذرية للمالية والمخزون وربط الحوالات.
5. بعض أخطاء best-effort يتم ابتلاعها.
6. Manifest يكتب بعض الحالات مباشرة ويتجاوز Lifecycle History.
7. إزالة الشحنة من Manifest قد تترك Loaded markers.
8. وجود حالات Canonical وLegacy.
9. Optimistic lock اختياري.
10. Receipt uniqueness مفروضة تطبيقياً فقط.
11. سباق `max()+1`.
12. حقول polymorphic بلا FK.
13. اختلاف نطاق `center_receipts`.
14. عدم تطابق بعض Permission Codes مع Route Guards.
15. أدوار مسماة فرعية لكنها تعامل Company‑Wide.
16. وجود `agent_user` دون `agent_id`.
17. Audit الحالي لا يحتوي Field Diffs كاملة.
18. Idempotency الحالية تعيد 409 ولا تعيد النتيجة السابقة.
19. Offline الحالي يغطي Ledger Draft فقط.
20. اختلاف Hard Delete وSoft Delete بين بعض جداول الطباعة والشحن.

لا توسع المهمة إلى إعادة بناء النظام كله بلا ضرورة، لكن لا تبنِ المزامنة فوق نقطة تهدد فقد البيانات أو تكرارها.

==================================================
17. التسجيل والمراقبة
==================================================

أنشئ Logs منظمة وآمنة لكل من:

- تشغيل Desktop.
- تشغيل Local Backend.
- اتصال PostgreSQL المحلي.
- Local migrations.
- Push sync.
- Pull sync.
- retries.
- conflicts.
- rejected operations.
- backups.
- recovery.

لا تسجل:

- كلمات المرور.
- Tokens كاملة.
- بيانات حساسة لا تحتاجها عملية التشخيص.

أضف Correlation IDs:

- operation ID.
- device ID.
- sync batch ID.
- central result ID.

وفر شاشة أو نافذة تشخيص إدارية تعرض:

- Local DB health.
- Central connection health.
- Pending count.
- Retry count.
- Conflict count.
- Oldest pending operation.
- Last push.
- Last pull.
- Current cursor.
- App/schema versions.
- زر تصدير Diagnostics منقح من الأسرار.

==================================================
18. الاختبارات الإلزامية
==================================================

أنشئ اختبارات Unit وIntegration وEnd‑to‑End حيث يلزم.

اختبر على الأقل:

1. إدخال 100 سطر دون إنترنت.
2. إغلاق التطبيق وإعادة فتحه، والتأكد من بقاء الأسطر.
3. إعادة تشغيل Windows واستمرار البيانات.
4. عودة الإنترنت ومزامنة جميع الأسطر.
5. فقد الرد بعد قبول العملية مركزيًا ثم إعادة المحاولة.
6. التأكد من عدم تكرار السجل مركزيًا.
7. الضغط المزدوج على زر الحفظ.
8. جهازان ينشئان سجلات في الوقت نفسه.
9. جهازان يعدلان السجل نفسه.
10. تغيير حالة مركزيًا أثناء وجود حدث محلي.
11. تعارض حقيقي وحفظه في جدول التعارضات.
12. حل التعارض بصلاحية إدارية.
13. انقطاع الإنترنت وسط Batch.
14. انهيار التطبيق أثناء SENDING.
15. إعادة تشغيل العامل واستعادة العمليات العالقة.
16. فشل عملية واحدة داخل Batch واستمرار النتائج المستقلة.
17. تأخر ساعة Windows أو تغييرها.
18. انتهاء صلاحية Offline Authentication.
19. تعطيل الجهاز مركزيًا.
20. سحب صلاحية فرع من المستخدم.
21. Scoped Snapshot لا يحتوي بيانات غير مسموحة.
22. Pull باستخدام Cursor.
23. Tombstone للحذف.
24. جهاز غائب مدة تتجاوز Retention ويحتاج Resnapshot.
25. فشل الترحيل المالي بعد نجاح Business Sync.
26. التأكد من عدم إعادة إنشاء الشحنة.
27. فشل Local Migration.
28. استعادة نسخة احتياطية.
29. تحديث التطبيق مع وجود Outbox غير مرفوع.
30. عدم فقد البيانات عند uninstall/update غير المكتمل.
31. النسبة المئوية لا تصل إلى 100% قبل ACK المركزي.
32. بطاقة المزامنة تعرض العدد الصحيح.
33. عدم تغير التصميم العام للواجهة.
34. الطباعة Offline إذا كانت لا تحتاج كتابة مركزية.
35. منع الإجراءات المركزية الحساسة Offline مع رسالة واضحة.

اختبر أحجامًا أكبر مثل:

- 1,000 عملية معلقة.
- 10,000 تغيير في Change Feed.
- Batches متعددة.
- اتصال بطيء ومتقطع.
- تكرار نفس Operation عشر مرات.

==================================================
19. خطة التنفيذ المرحلية
==================================================

نفّذ على مراحل مترابطة، مع Build واختبارات بعد كل مرحلة:

المرحلة 0: التدقيق والتثبيت المعماري

- فحص المشروع الحالي.
- فحص Desktop/Electron الموجود.
- فحص قاعدة البيانات والترحيلات.
- تحديد مسارات الكتابة الفعلية.
- إنشاء جدول تصنيف Local Writable / Read‑Only / Central‑Only.
- توثيق المخاطر دون تعديل التصميم.

المرحلة 1: Desktop Runtime وPostgreSQL المحلي

- تشغيل التطبيق كـWindows Desktop.
- إدارة Local Backend.
- إدارة PostgreSQL المحلي.
- Health checks.
- Local migrations.
- Device registration.

المرحلة 2: Local‑First Daily Ledger

- نقل حفظ جلسات وأسطر الدفتر إلى Local Backend.
- Transaction محلية تشمل Business Row + Outbox.
- الاستعادة بعد إعادة التشغيل.
- منع التكرار المحلي.
- الحفاظ على الواجهة الحالية.

المرحلة 3: Push Sync وDurable Results

- Central Sync API.
- Operation results.
- Device sequence.
- Retry/backoff.
- عدم التكرار.
- المزامنة الدفعية.

المرحلة 4: Pull وCentral Change Feed

- Change Feed.
- Cursor.
- Tombstones.
- Scoped Snapshot.
- Resnapshot policy.

المرحلة 5: التعارضات والآثار المركزية

- Conflict store.
- Resolution workflow.
- فصل Business Sync عن المالية والمخزون.
- Administrative audit.

المرحلة 6: واجهة حالة المزامنة

- البطاقة المنبثقة الصغيرة.
- النسبة الحقيقية.
- الأعداد والحالات.
- رسائل عربية دقيقة.
- دون إعادة تصميم.

المرحلة 7: النسخ الاحتياطي والاستعادة

- Backup مشفر.
- Restore workflow.
- حماية Outbox.
- Update/uninstall safeguards.

المرحلة 8: الاختبار والتغليف

- تنفيذ جميع سيناريوهات الاختبار.
- Windows installer.
- اختبار جهاز نظيف.
- اختبار Offline فعلي.
- اختبار مزامنة مع الخادم المركزي.
- توثيق النتائج بالأرقام.

لا تنتقل إلى مرحلة لاحقة إذا كان فشل المرحلة الحالية قد يؤدي إلى فقد أو تكرار البيانات.

==================================================
20. معايير القبول النهائية
==================================================

لا تعتبر المهمة مكتملة إلا إذا تحقق الآتي:

- التطبيق يعمل كتطبيق Windows Desktop.
- يمكن فتحه والعمل فيه دون إنترنت بعد التهيئة والمصادقة المسموحة.
- إدخال أسطر دفتر الشحن لا يحتاج اتصالًا بالخادم المركزي.
- كل سطر يُحفظ في PostgreSQL المحلي.
- Outbox يكتب في المعاملة نفسها.
- لا تضيع البيانات بعد الإغلاق أو إعادة تشغيل Windows.
- تعود المزامنة تلقائيًا عند عودة الإنترنت.
- إعادة المحاولة لا تنشئ سجلات مكررة.
- توجد مزامنة Push وPull.
- توجد Cursor وTombstones وScoped Snapshot.
- توجد معالجة صريحة للتعارضات.
- المالية والمخزون والحوالات المالية لا تُنفذ محليًا.
- يتم فصل Business Sync عن حالات المعالجة المالية.
- بطاقة المزامنة تعرض أرقامًا ونسبة حقيقية.
- لا تظهر 100% قبل ACK المركزي.
- الواجهة والتصميم الحاليان محفوظان.
- PostgreSQL المحلي غير مكشوف على الشبكة.
- الأسرار غير موجودة في الكود أو Logs.
- يوجد Backup وRestore للبيانات غير المزامنة.
- يعمل المثبت على Windows 10/11.
- جميع Builds والاختبارات المطلوبة ناجحة.
- تم إجراء اختبار Offline حقيقي، وليس Mock فقط.
- تم إجراء اختبار انقطاع الاتصال بعد قبول الخادم وقبل وصول الرد.
- تم إثبات عدم التكرار بنتائج فعلية.
- تم تسليم تقرير نهائي واضح.

==================================================
21. المخرجات المطلوبة
==================================================

في نهاية التنفيذ سلّم:

1. الكود الكامل للتطبيق المحلي ومحرك المزامنة.
2. جميع migrations المحلية والمركزية.
3. Central Sync API موثقًا.
4. مخطط جداول Outbox وOperation Results وChange Feed وConflicts.
5. جدول تصنيف جميع الكيانات:
   - Local Writable
   - Local Read‑Only
   - Central‑Only
6. توثيق بروتوكول Push/Pull.
7. توثيق Conflict Resolution.
8. توثيق التثبيت والتحديث والإزالة.
9. توثيق Backup/Restore.
10. تقرير اختبارات يتضمن:
    - اسم الاختبار.
    - النتيجة.
    - القياس الفعلي.
    - الأدلة.
11. قائمة الملفات المعدلة والجديدة.
12. قائمة migrations المطبقة.
13. المخاطر أو العناصر المتبقية إن وجدت.
14. ملف Windows Installer محلي.
15. المسار الكامل للمثبت.
16. حجم المثبت.
17. SHA‑256.
18. تقرير عربي داخل المستودع يوضح:
    - ما تم.
    - ما لم يتم.
    - ما هو مؤكد بالاختبار.
    - ما هو جزئي.
    - ما هو محجوب ولماذا.

==================================================
22. أسلوب العمل والتقرير
==================================================

- ابدأ بفحص الأدلة الفعلية، لا بالتخمين.
- استخدم المخطط الحي مصدرًا للحقيقة عند اختلافه عن migrations.
- لا تعرض أسرار الاتصال.
- لا تدّع نجاح اختبار لم تنفذه.
- اذكر القياسات الفعلية فقط.
- افصل في التقرير بين:
  - مكتمل.
  - جزئي.
  - محجوب.
- إذا وجدت قرارًا معماريًا جديدًا يغير نطاق العمل أو البيانات المالية أو الصلاحيات بصورة جوهرية، توقف قبل تنفيذه واشرح القرار المطلوب.
- لا تتوقف بسبب مشكلة بسيطة يمكن تشخيصها وإصلاحها ضمن النطاق.
- حافظ على جميع وظائف الويب الحالية ما لم يكن هناك سبب موثق لتعديلها.
- لا تجعل تطبيق سطح المكتب يكسر نسخة المتصفح الحالية.
- افصل مسارات Desktop/Offline عن Web عند الحاجة دون نسخ منطق الأعمال بصورة متضاربة.
- استخدم خدمات وقواعد أعمال مشتركة قدر الإمكان.
- نفّذ المهمة بجودة Production، وليس كنموذج تجريبي.

النتيجة النهائية المطلوبة:

موظف إدخال البيانات يستطيع إدخال أسطر الشحن طوال يوم العمل حتى لو انقطع الإنترنت مرارًا. يرى فورًا أن البيانات محفوظة محليًا، ويستطيع متابعة العمل دون انتظار. وعند عودة الاتصال، يرفع التطبيق العمليات تلقائيًا ويعرض عددها ونسبة تقدم حقيقية، مع ضمان عدم فقد البيانات وعدم تكرارها وعدم تنفيذ الآثار المالية أو المخزنية بصورة غير آمنة.